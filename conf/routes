# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /               controllers.HomeController.index(request: Request)
GET     /search         controllers.HomeController.search(request: Request)

GET     /assets/*file   controllers.Assets.versioned(path="/public", file: Asset)
GET     /source/:sourceName   controllers.SourceProfileController.profile(request: Request, sourceName: String, sourceLink: String, searchInput: String ?= null)
